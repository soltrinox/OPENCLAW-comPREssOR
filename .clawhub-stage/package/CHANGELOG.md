# Changelog

## 0.1.3

Portal docs refresh for ClawHub homepage: README rewritten (mechanism/outcome/scope, install, doctor CLI, dual-state figures); `docs/OPENCLAW.md` Gateway integration; `docs/value.md` honesty gate (no Cursor PERFORMANCE / absolute % product claims; measurement → `docs/RESEARCH.md`); shipped mechanism figures under `docs/figures/` (OC-05/08/09/10). Doctor remains `openclaw compressor doctor` on host `2026.7.1-2` (no `registerDoctorCheck`).

## 0.1.2

ClawHub compat: package renamed to `@soltrinox/openclaw-compressor`; `openclaw.extensions` → `./dist/index.js`; peer/pluginApi floor `>=2026.7.1-2`; install metadata for ClawHub; doctor checks via `openclaw compressor doctor` (no host doctor-check registrar on `2026.7.1-2`).

## 0.1.1

Package metadata and publish surface for `@soltrinox/openclaw-compressor` (scope `soltrinox`). Plugin/slot id remains `compressor`.

## 0.1.0

Sidecar engine path: ClawHub-shaped package, strict config schema, lifecycle/sidecar wiring, recall-0.5 Python knobs. Peer floor later raised to `openclaw` `>=2026.7.1-2`.
